#include <iostream>

void GetMatrix(int[], int*, int*);

int main() {
  int data[100], m, n;
  GetMatrix(data, &m, &n);
  return 0;
}

void GetMatrix(int value[], int* row, int* col) {
  // เขียนคำสั่ง
}
