#include <iostream>

int* GetMatrix(int*, int*);

int main() {
  int data[100], m, n;
  int* ptr;
  ptr = GetMatrix(&m, &n);
  return 0;
}

int* GetMatrix(int* row, int* col) {
  // เขียนคำสั่ง
  return nullptr;
}
